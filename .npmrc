# Changesets compatibility
enable-pre-post-scripts=true

# Storybook v10 requires hoisting for proper module resolution in pnpm workspaces
public-hoist-pattern[]=*storybook*

# npm registry auth (NPM_TOKEN set via CI secrets)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
